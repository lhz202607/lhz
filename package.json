{
  "name": "antique-game",
  "version": "1.0.0",
  "private": true,
  "description": "古董局中局·十二兽首 多人在线桌游",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "node backend/dist/src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
